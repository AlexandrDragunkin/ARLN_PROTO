//-- Простая проверка на наличие в сцене резинового шкафа
//-- Без панелей

getpar IDProto;

if sysvar(62)>0
{
	selbyattr "left(FurnType,5)==\"11111\"" partly all done;
	object_2=sysvar(61);
	if object_2>0
	{
		putmsg("В сцене уже присутствует резиновый шкаф",0)
		exit;
	}
}
ProtoObj add "Shkaf" IDProto;
