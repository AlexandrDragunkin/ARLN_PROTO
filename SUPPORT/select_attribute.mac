//-- Макропрограмма нужна для обезгеморроивания функций выбора объектов по атрибутам
//-- Входные параметры:
//-- sss - строка с фильтром.
//-- Дополнительно проверяет, есть ли в сцене хоть что-то

getpar sss;
=sss
//-- Если в сцене ничегонет - просто выходим
if (sysvar(60)==0)
{
  cancel;
}
select attribute sss done;
exit;
